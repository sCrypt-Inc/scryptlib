struct ST0<T> {
    int x;
    T y;
}

struct ST1<T> {
    T x;
    ST0<T> st0;
}

struct ST2 {
    int x;
}


contract C {
    
    ST1<ST2> a;
    ST1<int> b;
    public function unlock(ST1<ST2> Pa) {
        
        ST2 st2 = {1};

        ST0<ST2> st0 = {1, st2};

        ST1<int> a = {1, {1,3}};
        ST1<ST2> b = {st2, st0};
        require(true);
    }
}