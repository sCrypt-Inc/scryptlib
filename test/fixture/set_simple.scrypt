
contract C {

    public function unlock(int x) {
        HashedSet<int> set = new HashedSet(b'');
        SortedItem<int> val = {x, 0};
        require(set.add(val));
        require(set.has(val));
        require(true);
    }
}